# Dependencies
node_modules/
venv/
.venv/
__pycache__/
*.pyc

# Distribution / build outputs
dist/
build/
out/
*.tsbuildinfo
android/build/
ios/build/
Pods/
.expo/

# IDEs
.idea/
.vscode/
.cursor/
*.swp
*.swo

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Broader env variants (real secrets stay local)
.env.*
!.env.example
!**/.env.example

# Logs
*.log
*.logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
debug*.log

# OS
.DS_Store
Thumbs.db

# Local DBs & vector stores
ruvector.db
*.db
*.sqlite
*.sqlite3
.claude/memory.db
.swarm/memory.db
.code-review-graph/graph.db

# MCP / agent / graph tooling (local only)
.omc/
.agents/
.swarm/
.claude-flow/
.graphifyignore
graphify-out/

# CI / EAS
.eas-logs/

# Strapi-style temp & uploads (root-level copies)
.tmp/
.strapi/
/uploads/
public/uploads/

# Internal product & planning docs (never publish to GitHub)
# Catch-all: all Markdown stays local unless you force-add (don't).
*.md
docs/
.planning/
PRD.md
Swarup.md
ENGR_USER_JOURNEY.md
SYSTEM_ARCHITECTURE.md
CALL_FEEDBACK_MECHANISM.md
CALL_QUALITY_SCORE_SPEC.md
CLAUDE.md
AGENTS.md
CONTEXT.md

# Internal drafts & generated trees
archive/
generated/

# Project-specific
/generated/prisma

# Firebase secrets (service account private keys)
*-firebase-adminsdk-*.json
backend-nest/config/firebase-service-account.json
google-services (1).json
google-services (2).json
google-services (3).json

# Claude Code local helpers (runtime files, not for repo)
.claude/helpers/

# Git worktrees (local development only)
worktrees/

# Artillery local output (reports may contain run metadata)
backend-nest/scripts/load/results/*.json
backend-nest/scripts/load/results/*.html
