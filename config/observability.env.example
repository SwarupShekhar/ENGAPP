# Copy to repo root as .env.observability (gitignored) or export before compose up.
# Used by docker-compose.prod.yml grafana service.

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme-set-a-long-password
